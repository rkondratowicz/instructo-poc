{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Instruction Metadata",
  "type": "object",
  "required": ["tags"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "URI of the JSON Schema that describes the format of this JSON document"
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "Tags used for discovery and classification"
    },
    "description": {
      "type": "string",
      "description": "Short explanation used in catalogs or UIs"
    },
    "languages": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Optional list of languages this instruction is relevant to"
    }
  },
  "additionalProperties": false
}
